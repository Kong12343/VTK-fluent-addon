# Training extras (PyTorch Geometric / GraphSAINT baseline).
# Core deps (torch, torch-geometric, h5py) are in ../requirements.txt — install that first.
#
# Optional training accelerators (match torch major.minor + CUDA tag):
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
#
# Uncomment when available for your torch version:
# torch-sparse>=0.6.18
# pyg-lib>=0.3.0
#
# Without torch-sparse / pyg-lib, train_baseline_graphsaint.py falls back to random subgraph sampling.
