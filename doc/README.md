# 文档入口（兼容 `doc/`）

本仓库的主要文档目录是 `docs/`（复数）。之所以保留/新增 `doc/`，是为了避免使用者把目录记错。

## 从哪里开始读

请优先从 `docs/README.md` 开始，它包含：

- 文档索引（每份文档解决什么问题）
- 推荐阅读顺序
- 术语与约定（例如：文件内 1-based id vs 内部 0-based）

路径：`../docs/README.md`

